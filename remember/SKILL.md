---
description: 保存本次会话的关键记忆到本地记忆库，下次开启会话时自动回忆
allowed-tools: Write, Bash(mkdir*), Glob, Read
argument-hint: [可选：记忆主题/标签]
---

# /remember — 本地长期记忆

将本次会话的关键信息保存到本地记忆库。下次开启 Claude Code 时会自动读取相关记忆。

## 记忆存储位置

```
~/.claude/memory/
├── {项目名}/          # 项目专属记忆
│   └── YYYY-MM-DD_{slug}.md
└── global/            # 跨项目通用记忆
    └── YYYY-MM-DD_{slug}.md
```

## 项目名映射

根据当前工作目录自动判断（与 hooks/memory-recall.sh 中的映射保持一致）。
如果 $ARGUMENTS 包含 "global" 或 "全局"，存到 `global/` 目录。

**默认映射示例**（安装时按需修改 detect_project 函数）：
- `project-a` — 项目A
- `project-b` — 项目B
- `general` — 其他

## 执行步骤

1. **判断项目**：从当前 `pwd` 推断项目名
2. **生成摘要**：回顾本次会话，提取以下内容：
   - **关键决策**：做了什么架构/设计/技术选择
   - **修改文件**：改了哪些文件、为什么改
   - **发现/教训**：踩了什么坑、发现了什么规律
   - **待办**：未完成的事项
3. **写入文件**：用 Write 工具写入

## 记忆文件格式

```markdown
# {日期} {简短标题}

**项目**: {项目名}
**标签**: {2-4个关键词，逗号分隔}

## 关键决策
- {决策1}

## 修改文件
- `{文件路径}` — {改动说明}

## 发现/教训
- {教训1}

## 待办
- [ ] {未完成事项}
```

## 文件名规则

`YYYY-MM-DD_{slug}.md`

slug 从标题或 $ARGUMENTS 生成，用英文小写+连字符，如：
- `2026-02-19_font-upgrade.md`
- `2026-02-19_api-refactor.md`

## 重要

- 每条记忆控制在 **30 行以内**，精炼为主
- 不要保存代码片段，只保存决策和结论
- 如果目录不存在，先用 `mkdir -p` 创建
- 保存后告知用户文件路径
